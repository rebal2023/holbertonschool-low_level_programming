#include "main.h"
#include <stdio.h>
/**
*_abs - computes the absolute value of an integer
*@c: integer to be checked
*Return: 0
*/
int _abs(int c)
{
if (c < 0)
{
c = c * -1;
return (c);
}
else
{
return (c);
}
}
